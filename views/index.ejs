<!DOCTYPE html>
<html>
<head>
    <title>Dansa</title>
</head>
<body>
    <h1>Dansa</h1>
    <p>A dance game, greatly inspired by StepMania! Songs and steps are borrowed from <a href="http://stepmania.com">stepmania.com</a>.</p>

    <h2>Play!</h2>
    <form method="GET" action="play">
        <div class="form-group">
            <label>Song</label>
            <select name="songId">
                <% for(var i=0; i<songs.length; i++){ %>
                    <option>
                        <%=songs[i]%>
                    </option>
                <% } %>
            </select>
        </div>

        <div class="form-group">
            <label>Background iframe URL</label>
            <input type="text" name="bg" value="https://goote.ch/e496320b529a499bb6b5e65b95e4749c.project/" />
        </div>


        <div class="form-group">
            <input type="submit" value="PLAY" />
        </div>
    </form>

    <script src="/js/jquery-1.5.1.min.js" type="text/javascript"></script>
    <script src="//connect.soundcloud.com/sdk.js" type="text/javascript"></script>
    <script>
        SC.initialize({
            client_id: '<%-scClientId%>'
        });

        // Try getting a list of public songs with BPM defined
        SC.get('/tracks', {
            filter: 'public',
            bpm: {
                from: 40,
                to: 300
            },
            duration: {
                from: 60 * 1000
            }
        }, function (result){
            var $select = $('#scSelect');
            var $option = $('<option>test</option>');
            $select.append($option);
            console.log(result);

        });
    </script>
</body>
</html>